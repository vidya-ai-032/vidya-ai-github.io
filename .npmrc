# Disable binary downloads for problematic packages
lightningcss-linux-x64-gnu=0
# Force JS implementation instead of native
node-linker=hoisted
# Skip optional dependencies (many native modules)
omit=optional